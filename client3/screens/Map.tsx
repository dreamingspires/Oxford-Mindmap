import React from 'react';
import { Text, Button, View } from 'react-native'

export const MapScreen = (props) => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text style={{ fontSize: 30 }}>Map Screen Placeholder</Text>
        </View>
    );
}
